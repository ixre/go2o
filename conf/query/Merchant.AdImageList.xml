<?xml version="1.0" encoding="utf-8" ?>
<ExportItemConfig>
  <ColumnMappingString></ColumnMappingString>
  <Query>
    <![CDATA[SELECT ad_image_ad.* FROM ad_image_ad
     INNER JOIN ad_list ON ad_list.id = ad_image_ad.ad_id
     WHERE merchant_id={merchant_id} AND ad_id={ad_id}
    AND ({enabled}<0 OR ad_image_ad.enabled={enabled})
     AND title LIKE '%{keyword}%'
    ORDER BY ad_image_ad.sort_number limit {page_start},{page_size}
     ]]>
  </Query>
  <Import><![CDATA[]]></Import>
  <Total>
    <![CDATA[SELECT COUNT(0) FROM ad_image_ad
     INNER JOIN ad_list ON ad_list.id = ad_image_ad.ad_id
     WHERE merchant_id={merchant_id} AND ad_id={ad_id}
    AND ({enabled}<0 OR ad_image_ad.enabled={enabled})
     AND title LIKE '%{keyword}%']]>
  </Total>
</ExportItemConfig>
